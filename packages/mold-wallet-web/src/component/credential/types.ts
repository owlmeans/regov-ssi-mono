import {
  REGISTRY_SECTION_OWN,
  REGISTRY_TYPE_CREDENTIALS
} from "@owlmeans/regov-ssi-core"


export const CREDENTIAL_LIST_ROUTE = `/credential/list/${REGISTRY_TYPE_CREDENTIALS}/${REGISTRY_SECTION_OWN}`