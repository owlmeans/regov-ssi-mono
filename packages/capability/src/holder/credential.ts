import { WalletWrapper } from "@owlmeans/regov-ssi-core";


export const holderCapabilityHelper = (wallet: WalletWrapper) => {
  return {
    claim: () => {
      return {
        
      }
    }
  }
}